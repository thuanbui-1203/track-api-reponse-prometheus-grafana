
global:
  scrape_interval: 5s
  evaluation_interval: 5s

  external_labels:
    monitor: demo

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: demo-app
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]
        labels:
          service: demo-app
          env: demo

# # ── the new domain ──
# - job_name: other-domain          # information of another domain
#   scheme: https                   
#   metrics_path: /metrics          # could be different endpoint
#   scrape_interval: 30s            # optional per-job override
#   scrape_timeout: 10s             # must be <= scrape_interval
#   basic_auth:                     # if it needs auth
#     username: metrics                                   # authentication
#     password_file: /etc/prometheus/other-domain.pw
#   static_configs:
#     - targets: ["api.example.com:443"]                  # target measurement api
#       labels:
#         service: other-api
#         env: prod
#         region: ap-southeast-1